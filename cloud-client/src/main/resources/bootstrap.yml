spring:
  profiles:
    active: local
  application:
    name: cloudClient
---
spring:
  profiles: local
  cloud:
    config:
      uri: http://localhost:8888
      username: userName
      password: passw0rd
