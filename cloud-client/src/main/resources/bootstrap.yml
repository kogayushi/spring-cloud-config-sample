spring:
  profiles:
    active: local
  application:
    name: cloudClient
  cloud:
    config:
      fail-fast: true
---
spring:
  profiles: local
  cloud:
    config:
      uri: http://localhost:8888
      username: userName
      password: passw0rd
