server:
  port: 8888

---
spring:
  profiles: local
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kogayushi/spring-cloud-config-sample-repo.git
      fail-fast: true

security:
  user:
    name: userName
    password: passw0rd
